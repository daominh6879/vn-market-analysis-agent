[pytest]
pythonpath = .
markers =
    integration: cần Qdrant + Ollama + MinIO + Postgres đang chạy
    e2e: calls real LLM (slow, costs tokens) — opt-in with -m e2e
    live: hits real external APIs (vnstock, CafeF, VCB) — do NOT run in CI
